#include "main.h"
/**
 * time_table- prints the last digit of a number
 *
 * Return: (0)
 */
void times_table(void)
	{
	int x, y, z;

	for (x = 0 ; x < 10 ; i++)
{
	for (y = 0 ; y < 10 ; j++)
{
	a = x * y;
	if (y == 0)
	_putchar(a + '0');
	if (a < 10 && !y == 0)
{
	_putchar(',');
	_putchar(' ');
	_putchar(' ');
	_putchar(k + '0');
}
	if (a >= 10)
{
	_putchar(',');
	_putchar(' ');
	_putchar(a / 10 + '0');
	_putchar(a % 10 + '0');
}
}
	_putchar('\n');
}
}
